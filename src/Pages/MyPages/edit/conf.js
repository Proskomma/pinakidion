import Edit from './Edit';

const conf = {
  url: "edit",
  menuEntry: "Edit",
  pageTitle: "Scripture Editor",
  description: "A Scripture Editor",
  pageClass: Edit,
  state: [
    ['documentId', null],
  ],
};

export default conf;
